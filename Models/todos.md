# TODOs

Need to check if all the models pass IOs
Need to add parameters that each model uses 
Ctv needs some changes and some better understanding:
    1. How do they do unsupervised loss? Do they propagate it to the model?
    2. Do we also use the raw loss? 
    3. This might need to modify the dataset object